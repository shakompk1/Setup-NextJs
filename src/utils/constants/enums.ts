export enum Language {
  uk = 'az',
  ru = 'ru',
}

export enum ThemeStyle {
  dark = 'dark',
  light = 'light',
}

export enum HTTPTypes {
  get = 'get',
  post = 'post',
  delete = 'delete',
  put = 'put',
}
